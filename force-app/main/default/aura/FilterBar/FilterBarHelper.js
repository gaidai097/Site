({
    helperSubmit : function( nameFilter, dateFilter, salaryFilter ){
        
		console.log('nameFilter ' + nameFilter + ' dateFilter ' + dateFilter  + ' salaryFilter ' + salaryFilter );
        
	}
})